table:
  name: action_logs
  schema: app
object_relationships:
  - name: action
    using:
      manual_configuration:
        column_mapping:
          action_id: id
        insertion_order: null
        remote_table:
          name: actions
          schema: app
  - name: event
    using:
      manual_configuration:
        column_mapping:
          event_id: id
        insertion_order: null
        remote_table:
          name: events
          schema: app
select_permissions:
  - role: app
    permission:
      columns:
        - id
        - app_id
        - trace
        - event_log_id
        - payload
        - action_id
        - job_id
        - created_at
        - event_id
      filter:
        app_id:
          _eq: X-Hasura-App-Id
      limit: 100
